#home-index
  %nav.navbar.navbar-light.navbar-top.navbar-fixed-top.bg-light
    .container-fluid
      .container
        .navbar-header
          .navbar-brand
            = show_svg("Logo2.svg")
        %ul.nav.navbar-nav.navbar-right
          - if current_user.has_role?(:admin)
            %li= link_to('Novo Usuário', new_user_path)
          %li= link_to('Novo Produto', new_product_path)
          - if user_signed_in?
            %li= link_to('Logout', destroy_user_session_path, :method => :delete)
          - else
            %li= link_to('Login', new_user_session_path)
